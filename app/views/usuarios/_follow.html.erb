<%= form_for current_user.relationships.find_by_followed_id(@usuario), :html => { :method => :delete }, :remote => false do |f| %>
<%= f.submit "Dejar de seguir", class: :"btn btn-large btn-primary active"%>
<% end %>



