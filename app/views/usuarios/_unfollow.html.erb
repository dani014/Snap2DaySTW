<%= form_for current_user.relationships.build(:followed_id => @usuario.id), :remote => false do |f| %>
<%= f.hidden_field :followed_id %>
<%= f.submit "Seguir", class: :"btn btn-large btn-primary active"%>
<% end %>
